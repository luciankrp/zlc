projection;
strict ( 2 );
use draft;

define behavior for ZLC_C_CONTACTTP alias Contact
{
//  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use action createAddress;
  use action createCustomer;
  use action createEmployee;
}
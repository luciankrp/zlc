projection;
strict ( 2 );
use draft;
define behavior for ZC_SO //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Prepare;
  use action Resume;
}